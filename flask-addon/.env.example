# CORS許可（Open WebUI のURLを設定推奨。カンマ区切りで複数可）
# 例: http://localhost:3000,https://webui.internal.example.com
CORS_ALLOW_ORIGINS=*

APP_NAME=flask-addon
LOG_LEVEL=INFO

# 疑似埋め込みの次元数（将来の本格埋め込みに合わせて変更）
VECTOR_DIM=384
MAX_CORPUS=100000
